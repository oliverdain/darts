* Some tests to be sure things are working!

* The stylesheet is a .less stylesheet but currently isn't using *any* LESS goodness (created it before I added LESS). Reduce redundancy via LESS.

* Better visual cues about the current state of the app (historical mode vs. current mode)

